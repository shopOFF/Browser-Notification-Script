# Browser-Notifications-Script
### Script that notifies when a change is made to a html element.
### Run the script in the browser's console.
### Console log's a message and beep's when a change is made to a desired html element.
